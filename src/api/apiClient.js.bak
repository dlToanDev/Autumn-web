import axios from 'axios'

// apiClient: diem vao duy nhat de goi backend.
// Ly do tach rieng:
// - De doi baseURL/header/timeout o mot noi duy nhat.
// - Giup page chi con tap trung vao hien thi UI.
const apiClient = axios.create({
  // baseURL de rong vi Vite dang proxy '/api' sang backend trong vite.config.js.
  baseURL: '',
})

export default apiClient
